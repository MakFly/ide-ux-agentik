# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# instant-grep index
.ig/

# Playwright
test-results/
playwright-report/
playwright/.cache/

# Codex runtime (contains auth.json — never commit)
.codex-home/

# Task worktrees (created by task.start). Vite watches the project root and
# would full-reload the page on every dispatch if these aren't ignored.
.multica/

# Local scratch
hello.py
